#
#  release.yml
#  LearnCHANGELOG
#
#  Created by west on 30/11/25.
#  Updated: fix release-please + Info.plist update flow
#

name: Release Changelog

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
<<<<<<< Updated upstream
    runs-on: ubuntu-latest
=======
    runs-on: macos-15
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      tag_name: ${{ steps.release.outputs.tag_name }}
      major: ${{ steps.release.outputs.major }}
      minor: ${{ steps.release.outputs.minor }}
      patch: ${{ steps.release.outputs.patch }}

>>>>>>> Stashed changes
    steps:
      - uses: googleapis/release-please-action@v4
        id: release
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
          target-branch: main
          token: ${{ secrets.GITHUB_TOKEN }}

<<<<<<< Updated upstream
      # Optional: Tampilkan output untuk debugging
      - name: Show Release Output
          if: steps.release.outputs.release_created
          run: |
            echo "Release created: ${{ steps.release.ouputs.release_created }}"
            echo "Tag name: ${{ steps.release.outputs.tag_name }}"
            echo "Version: ${{ steps.release.outputs.major }}.${{ steps.release.outputs.minor }}.${{ steps.release.outputs.patch }}"
            echo "Upload URL: ${{ steps.release.outputs.upload_url }}"
=======
      - name: Checkout code
        if: steps.release.outputs.release_created
        uses: actions/checkout@v4
        with:
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}

      # Update Info.plist dengan versi baru setelah release dibuat
      - name: Update Info.plist version
        if: steps.release.outputs.release_created
        run: |
          VERSION="${{ steps.release.outputs.major }}.${{ steps.release.outputs.minor }}.${{ steps.release.outputs.patch }}"
          echo "Updating Info.plist to version $VERSION"
          /usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString $VERSION" LearnCHANGELOG/Info.plist
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add LearnCHANGELOG/Info.plist
          git commit -m "chore: update Info.plist to $VERSION [skip ci]"
          git push

      - name: Show Release Output
        if: steps.release.outputs.release_created
        run: |
          echo "Release created: ${{ steps.release.outputs.release_created }}"
          echo "Tag name: ${{ steps.release.outputs.tag_name }}"
          echo "Version: ${{ steps.release.outputs.major }}.${{ steps.release.outputs.minor }}.${{ steps.release.outputs.patch }}"
>>>>>>> Stashed changes
